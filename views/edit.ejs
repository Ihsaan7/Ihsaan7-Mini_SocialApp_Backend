<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Signup Form</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
        }
    </style>
</head>

<body class="bg-gray-900  min-h-screen text-white">
    <div class="mt-8">
        <div class=" h-fit flex mt-10">
            <div class="px-10 py-6">
                <div class="mb-2">
                    <p class="text-3xl ">You can Update.</p>
                </div>
                <div class="mb-20 w-[400px]">
                    <form method="post" action="/edit/<%= post._id%>" class="flex flex-col  w-[500px]">
                        <textarea name="content" placeholder="What's on your mind?"
                            class="w-full resize-none bg-transparent border-2 border-gray-700 rounded-md p-4 py-6"><%= post.content %></textarea>
                        <button type="submit"
                            class="w-fit px-4 py-2 mt-2 rounded-md bg-yellow-500 text-black hover:cursor-pointer">Update
                            Post</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>

</html>